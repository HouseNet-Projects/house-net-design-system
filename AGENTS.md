# HouseNet design-system agent map

This repository is governed by `HouseNet-Projects/house-net-control-plane`.
Read `house-net-control.json`, load the canonical control plane, run `bin/housenet-preflight --json` there, and validate this repository with the pinned reusable policy gate before meaningful work.
Human-facing content must contain complete English and Armenian sections. Machine tokens, schemas, code and agent instructions may remain English-only.
Owner approval is required for repository configuration, reclassification and destructive actions.
Before editing a version, read this repository's canonical release manifest and use `bin/housenet-version`; never manually synchronize version strings or confuse own and consumed component versions.
